
/**
 * Placeholder function to map a specific step text to a video URL.
 * Currently returns null for all steps.
 */
export const getVideoUrlForStep = (stepText: string, context: string): string | null => {
  // Future implementation:
  // if (stepText.toLowerCase().includes('hardware wallet')) return 'https://youtube.com/...';
  
  return null;
};
